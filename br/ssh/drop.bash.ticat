help = remove instances' backupped data dir: <dir>.<tag>

[arg]
br-backup-tag|backup-tag|tag = ''
cluster-name|cluster|name|n = ''
deploy-user = root

[arg2env]
tidb.data.tag = br-backup-tag
tidb.cluster = cluster-name
deploy.user = deploy-user

[env]
tidb.data.tag = read
tidb.cluster = read
deploy.user = read

[dep]
tiup = to get dir of each instance from cluster
