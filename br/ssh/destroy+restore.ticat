help = stop and clean a TiDB cluster, restore data by dir via ssh, then start cluster
abbr = remove+restore|delete+restore|rm+restore|del+restore|rm+rs|del+rs
trivial = 1

flow = tidb.destroy : tidb.deploy : br.ssh.restore : tidb.start
