help = kill a tikv node randomly
abbr = 

[arg]
cluster-name|cluster|name|N = ''

[arg2env]
tidb.cluster = cluster-name

[env]
tidb.cluster = read
tidb.node.killed.host = write
tidb.node.killed.port = write

[dep]
tiup = to deploy cluster
jq = parse tiup's output
