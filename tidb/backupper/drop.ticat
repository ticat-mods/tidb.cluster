help = delete cluster's backupped data by tag
trivial = 3

flow = [[tidb.backupper]].drop

[arg]
tag|t = ''
cluster-name|cluster|name|n = ''

[arg2env]
tidb.data.tag = tag
tidb.cluster = cluster-name

[env]
tidb.backupper = read
